{
    "inputs": [{"name": "input", "type": "string_list", "format": "string_list"}],
    "outputs": [{"name": "output", "type": "boolean", "format": "boolean"}],
    "script": "import six; output = isinstance(input, list) and all(isinstance(elm, six.string_types) for elm in input)",
    "mode": "python"
}
