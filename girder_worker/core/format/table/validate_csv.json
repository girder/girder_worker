{
    "inputs": [{"name": "input", "type": "table", "format": "csv"}],
    "outputs": [{"name": "output", "type": "boolean", "format": "boolean"}],
    "extensions": ["csv"],
    "script": "import six; output = isinstance(input, six.string_types)",
    "mode": "python"
}
